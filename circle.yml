machine:
  timezone: Asia/Tokyo
  services: 
    - docker
  environment:
  ruby:
    version: 2.2.3
dependencies:
  pre:
    - bundle exec knife solo -o dstat
test:
  override:
    - echo Hello World
